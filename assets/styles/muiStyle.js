/**
 * This file is only for styling mui components
 */
import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles({
    field: {
        display: "block",
        marginLeft: 10,
        marginBottom: 25,
        flexDirection: 'row',
    }
})

export default useStyles;
